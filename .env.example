# ========================================
# Configuration de la Base de Données
# ========================================
POSTGRES_USER=myuser
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=mydatabase
DATABASE_URL=postgres://myuser:your_password_here@db:5432/mydatabase

# ========================================
# Configuration Backend
# ========================================
NODE_ENV=development          # development ou production (mode backend)
BACKEND_PORT=3001

# Secrets pour JWT (générez une clé aléatoire forte)
JWT_SECRET=your_jwt_secret_key_here
ENCRYPTION_KEY=your_encryption_key_here

# ========================================
# Configuration Frontend
# ========================================
FRONT_MODE=development        # development ou production (contrôle DevDebugPanel)
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:3001/api  # URL de l'API backend (accessible dans le frontend)

# ========================================
# Notes Importantes
# ========================================
# - NODE_ENV contrôle le mode du backend (development/production) - Standard Node.js
# - FRONT_MODE contrôle le mode du frontend (development/production)
# - FRONT_MODE est exposé comme VITE_NODE_ENV dans le frontend via vite.config.js
# - Seules les variables avec le préfixe VITE_ sont accessibles côté frontend
# - VITE_API_URL doit pointer vers l'URL complète de l'API (/api inclus)
# 
# Exemples :
# - Dev complet        : NODE_ENV=development, FRONT_MODE=development
# - Prod complète      : NODE_ENV=production, FRONT_MODE=production
# - Debug backend only : NODE_ENV=development, FRONT_MODE=production (cache le DevDebugPanel)
# - Debug frontend only: NODE_ENV=production, FRONT_MODE=development (montre le DevDebugPanel)

# ========================================
# Services Externes - Twilio
# ========================================
TWILIO_ACCOUNT_SID=your_twilio_sid_here
TWILIO_AUTH_TOKEN=your_twilio_token_here

# ========================================
# Services Externes - AWS
# ========================================
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_S3_BUCKET_NAME=your-bucket-name-here
AWS_REGION=eu-west-1
